package com.sovereignx1.jquizzer.data.prompt;

public interface IQuizData {
    /**
     * We need to evaluate how we want to store the quiz/flashcard data.
     *
     * I'm currently thinking we refactor to get rid of the entire jquizzer.data.prompt package
     * Flashcard/Quiz Data classes should be all we need
     */
}
